[package]
name = "station-data"
version = "0.1.0"
edition = "2024"

[lib]
name = "station_data"
path = "src/lib.rs"

[dependencies]
contest-engine = { git = "https://github.com/chadsbrown/contest-engine" }
thiserror = "2"
